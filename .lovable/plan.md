
# خطة تحديث منصة طبخات

## ملخص التغييرات المطلوبة

سأقوم بتنفيذ 7 تغييرات رئيسية على المنصة:

1. **إخفاء زر "تسوق الطعام" من حساب الطباخ** - في الصفحة الرئيسية
2. **تصغير أيقونة الواتساب** + إضافة تسمية "تواصل مع طبخات"
3. **تحديث اسم المنصة في المرشد الذكي** - من "طبخاتي" إلى "طبخات"
4. **حذف جملة تأكيد الطلب** في سلة المشتريات
5. **إضافة خط "تواصل مع الطباخة"** فوق أزرار التواصل في صفحة الطباخ العامة
6. **تحديث أسعار الاشتراك** - CliQ: 10 دنانير، Stripe: 15 دولار
7. **تفعيل Stripe** وربطه بالمفاتيح المقدمة

---

## التفاصيل التقنية

### 1. إخفاء زر "تسوق الطعام" من حساب الطباخ

**الملف:** `src/pages/Index.tsx`

**التغيير:** تعديل قسم أزرار الطباخ (السطور 212-234) ليظهر فقط زر "مطبخي" بدون زر "تسوق الطعام"

```text
قبل:
- زران للطباخ: "تسوق الطعام" + "مطبخي"

بعد:
- زر واحد للطباخ: "مطبخي" فقط
```

---

### 2. تصغير أيقونة الواتساب + تسمية جديدة

**الملف:** `src/pages/Index.tsx`

**التغييرات:**
- تصغير الأيقونة من `w-12 h-12` إلى `w-10 h-10`
- تصغير الأيقونة الداخلية من `h-6 w-6` إلى `h-5 w-5`
- إضافة تسمية أسفل الزر: "تواصل مع طبخات"

```text
الشكل الجديد:
┌─────┐
│ 💬  │  ← أيقونة أصغر
└─────┘
تواصل مع طبخات ← تسمية جديدة
```

---

### 3. تحديث اسم المنصة في المرشد الذكي

**الملف 1:** `supabase/functions/ai-guide/index.ts`

**التغيير في systemPrompt:**
- تغيير "طبخاتي" إلى "طبخات" في عنوان المنصة
- تحديث جميع الإشارات للاسم الجديد

**الملف 2:** `src/hooks/useLanguage.tsx`

**التغييرات في الترجمات:**
- `aiGuide.title`: من "مرشد طبخاتي" إلى "مرشد طبخات"
- `aiGuide.welcome`: تحديث رسالة الترحيب

---

### 4. حذف جملة تأكيد الطلب من السلة

**الملف:** `src/pages/Cart.tsx`

**التغيير:** حذف الفقرة في السطور 364-366

```jsx
// سيتم حذف هذا:
<p className="text-sm text-muted-foreground text-center mt-4">
  {t('cart.contactCookToOrder')}
</p>
```

---

### 5. إضافة خط "تواصل مع الطباخة" في صفحة الطباخ العامة

**الملف:** `src/pages/PublicPage.tsx`

**التغيير:** إضافة عنوان مزخرف فوق أزرار التواصل (قبل السطر 267)

```jsx
{/* خط جديد فوق الأزرار */}
<div className="flex items-center justify-center gap-3 mb-4">
  <div className="h-px w-12 bg-border" />
  <span className="text-sm font-semibold text-muted-foreground">
    تواصل مع الطباخة
  </span>
  <div className="h-px w-12 bg-border" />
</div>
```

---

### 6. تحديث أسعار الاشتراك

**الملف:** `src/components/dashboard/PaymentManager.tsx`

**التغييرات:**
- **سعر CliQ:** من 5 دنانير إلى **10 دنانير**
  - السطر 102: `amount: 10`
  - السطر 341: `10 {t('payment.jod')}`
  
- **سعر Stripe:** من 5 دنانير إلى **15 دولار**
  - تحديث العرض ليظهر 15 دولار

---

### 7. تفعيل Stripe

**التغييرات:**

**أ) إضافة مفاتيح Stripe كـ Secrets:**
- `STRIPE_PUBLISHABLE_KEY`: pk_test_51SwOn3IIzXmlPxz9...
- `STRIPE_SECRET_KEY`: sk_test_51SwOn3IIzXmlPxz9...

**ب) تحديث PaymentManager.tsx:**
- إزالة حالة "مغلق حالياً" من Stripe
- تفعيل زر الدفع
- إضافة تكامل Stripe checkout

**ج) إنشاء Edge Function للـ Stripe:**
- إنشاء `supabase/functions/create-checkout/index.ts`
- معالجة إنشاء جلسة دفع Stripe

---

## ملخص الملفات المتأثرة

| الملف | نوع التغيير |
|-------|-------------|
| `src/pages/Index.tsx` | تعديل (أزرار + واتساب) |
| `src/pages/Cart.tsx` | تعديل (حذف جملة) |
| `src/pages/PublicPage.tsx` | تعديل (إضافة عنوان) |
| `src/components/dashboard/PaymentManager.tsx` | تعديل (أسعار + Stripe) |
| `src/hooks/useLanguage.tsx` | تعديل (ترجمات) |
| `supabase/functions/ai-guide/index.ts` | تعديل (اسم المنصة) |
| `supabase/functions/create-checkout/index.ts` | **جديد** (Stripe) |

---

## ملاحظات مهمة

1. **مفاتيح Stripe المقدمة هي للاختبار (test)** - للإنتاج ستحتاج مفاتيح live
2. سيتم حفظ مفاتيح Stripe في Secrets بشكل آمن
3. العميل سيتم توجيهه لصفحة Stripe checkout ثم العودة للمنصة بعد الدفع
