
# خطة التحسينات المطلوبة

## ملخص التغييرات

سأقوم بتنفيذ عدة تحسينات على المنصة تشمل:

1. **إضافة اختيار رمز الدولة في حقل الواتساب** (لوحة تحكم الطباخ)
2. **إظهار خيارات الاتصال بعد تأكيد الطلب** (موجود بالفعل - سأتحقق من عمله)
3. **إضافة رابط لصفحة الطباخ في السلة** (جنب كل طبق)
4. **تبسيط الصفحة الرئيسية** وإزالة قسم "ابدأ مشروعك الآن"
5. **أزرار ديناميكية حسب نوع المستخدم** (طباخ = تسوق الآن / مطبخي، زائر = سجلي كطباخة / تسوق الآن)
6. **تغيير لون "تجربة مجانية لمدة شهر"** إلى الأحمر

---

## التفاصيل التقنية

### 1. حقل الواتساب مع اختيار رمز الدولة

**الملف:** `src/components/dashboard/ProfileForm.tsx`

- استبدال حقل الواتساب العادي (`Input`) بمكون `PhoneInput` الموجود
- يتيح للطباخ اختيار رمز دولته عند إدخال رقم الواتساب

### 2. رابط صفحة الطباخ في السلة

**الملف:** `src/pages/Cart.tsx`

- إضافة زر/رابط صغير بجانب كل طبق في السلة
- الزر يفتح صفحة الطباخ العامة (`/p/[slug]`)
- لا يحتاج العميل تسجيل حساب لمشاهدة الصفحة

**التغييرات:**
- إضافة حقل `merchant_slug` لكل عنصر في السلة
- جلب الـ slug عند إضافة الطبق للسلة
- عرض أيقونة/زر للانتقال لصفحة الطباخ

### 3. تبسيط الصفحة الرئيسية

**الملف:** `src/pages/Index.tsx`

**إزالة:**
- قسم "🍳 ابدأ مشروعك الآن!" كاملاً (الأسطر 170-190)
- النص "انضم لمنصة طبخاتي وحوّل شغفك بالطبخ إلى مصدر دخل حقيقي"
- زر "ابدأي الآن" في الأسفل

### 4. الأزرار الديناميكية حسب نوع المستخدم

**الملف:** `src/pages/Index.tsx`

**المنطق:**
```text
if (not logged in):
    - زر "سجلي الآن كطباخة" (أخضر) + "تسوق الآن" (أحمر)
else if (merchant):
    - زر "تسوق الآن" + "مطبخي" (يذهب للوحة التحكم)
else if (customer):
    - زر "تسوق الآن" فقط
```

### 5. تغيير لون النص الفرعي

**الملف:** `src/pages/Index.tsx`

- تغيير `text-success` إلى `text-destructive` (أحمر)
- للنص "+ تجربة مجانية لمدة شهر"

---

## تحديث واجهة السلة (useCart)

**الملف:** `src/hooks/useCart.tsx`

- إضافة حقل `merchant_slug` في `CartItem` interface
- تمريره عند إضافة العنصر

**الملف:** `src/pages/Browse.tsx`

- تمرير `merchant_slug` عند استدعاء `addItem`

---

## الترجمات المطلوبة

**الملف:** `src/hooks/useLanguage.tsx`

إضافة:
- `'index.myKitchen': { ar: 'مطبخي', en: 'My Kitchen' }`
- `'cart.viewCookPage': { ar: 'زيارة صفحة الطباخ', en: 'Visit Cook Page' }`

---

## ملخص الملفات المعدلة

| الملف | نوع التغيير |
|-------|-------------|
| `src/pages/Index.tsx` | حذف قسم + أزرار ديناميكية + تغيير لون |
| `src/pages/Cart.tsx` | إضافة رابط صفحة الطباخ |
| `src/pages/Browse.tsx` | تمرير merchant_slug للسلة |
| `src/hooks/useCart.tsx` | إضافة حقل merchant_slug |
| `src/hooks/useLanguage.tsx` | إضافة ترجمات جديدة |
| `src/components/dashboard/ProfileForm.tsx` | استخدام PhoneInput للواتساب |

---

## النتيجة المتوقعة

1. الطباخ يستطيع إدخال رقم واتساب مع اختيار رمز الدولة
2. العميل يرى زر للانتقال لصفحة الطباخ بجانب كل طبق في السلة
3. الصفحة الرئيسية أبسط وأنظف بدون قسم التسجيل الإضافي
4. الأزرار تتغير حسب حالة المستخدم (زائر/طباخ/عميل)
5. نص التجربة المجانية يظهر باللون الأحمر
